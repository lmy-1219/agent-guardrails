# 同事册 —— 「我这次改动会不会牵动别人」的机械载体。
#
# ⭐ 它解决的问题：多个项目/多个窗并行时，**改完才发现动了别人的地基**。
#    ⛔ 靠自觉记不住，也⛔ 不该等使用者来问「要不要联动」。
# ⇒ 在这里登记项目之间的关系，开窗时会自动注入到模型眼前。
#
# ⚠️ 本文件默认**是空的**（下面全是注释）⇒ 同事册功能此刻是**关着**的。
#    这是故意的：独行侠是常态，⛔ 不该硬塞一份假名单。
#    要用就把下面解开注释改成你自己的项目。
#
# 📍 它住在插件的数据目录里（和你的规矩、日志同级），⭐ 升级插件⛔ 不会覆盖它。

# 项目:
#   我的后端:
#     别名: [backend, my-backend, 后端]     # 两台机文件夹名不同也认得出
#   我的前端:
#     别名: [frontend, web]
#   共用组件库:
#     别名: [ui-kit]
#
# 关系:
#   - 谁: [我的后端, 我的前端]
#     关系: 接口上下游（后端定契约，前端照着做）
#     改动要想到: |
#       改了接口字段/返回结构 ⇒ 送信给前端，⛔ 别等它自己发现 404。
#   - 谁: [共用组件库, 我的前端]
#     关系: 基建与使用方
#     改动要想到: |
#       改了组件的 props 或默认样式 ⇒ 先看有没有人在用，再决定要不要留兼容。

# ⭐ 填好之后，开窗时模型会看到一段「同事册」，里面直接给出**发信的完整命令**
#    （路径是插件按本机算出来的，⛔ 不用你记）。
