# 档位规划表 —— 「这个活用哪个模型、什么推理档位、什么沙箱权限」写死在这里。
#
# ⭐⭐ 这份文件是本包存在的**全部理由**：
#   ⛔ 不是为了多一个配置文件，而是为了让「档位规划」变成**显式配置**，
#   ⛔ 不靠模型每次临场发挥——临场发挥的后果是整队按最贵的档跑。
#   ⚠️ 真实事故：一次本该派出去的体力活，主窗自己拉了 6 个顶配子代理，
#     烧掉 63 万 token 才被发现。⇒ 档位⛔ 不能是"想到哪算哪"。
#
# 📍 放哪：把本文件改名成 `工作流.yaml`，放进**你项目**的配置目录：
#     <你的项目>/.claude/工作流.yaml
# ⚠️ 没有这个文件 ⇒ 派单引擎退回内置的保守缺省，且提醒你「该不该派单」的那条规矩
#    会自动噤声（它的生效条件就是「本项目已配派单」）⇒ 等于这条产线关着。

预设:
  # 铺开找东西：便宜、只读、⛔ 不许改文件
  抓取: {主模型: gpt-5.6-luna, 主档: low,    子模型: gpt-5.6-terra, 子档: low, fork: none, 沙箱: read-only}
  # 读代码理解现状：同上，稍微贵一点
  勘察: {主模型: gpt-5.6-luna, 主档: low,    子模型: gpt-5.6-terra, 子档: low, fork: none, 沙箱: read-only}
  # 独立复核／挑毛病：要能想，仍然只读
  审计: {主模型: gpt-5.6-terra, 主档: medium, 子模型: gpt-5.6-terra, 子档: low, fork: '3', 沙箱: read-only}
  # 真动手改东西：唯一允许写的一档
  实现: {主模型: gpt-5.6-sol,   主档: medium, 子模型: gpt-5.6-terra, 子档: low, fork: '3', 沙箱: workspace-write}

# 跨窗信箱落哪个目录（收信提醒按这个找）
收件目录: _dev/收件

# ⛔ 独占资源：声明「哪条命令会去用什么独占的外部程序」（如 Photoshop/Blender）。
#   ⚠️ 不声明 ⇒ 卡住哨和占用卫兵**完全不工作**，而那跟"平安无事"长得一样。
#   例：
# 独占资源:
#   - {程序: Photoshop, 命令含: 'slice_psd.py', 正常时长分钟: 12}

# ⭐ 真源根：执行方跨出这些目录要在体检里看得见（⛔ 不配就是不限制）
真源根:
  默认: {}
闸: []
收尾哨附加: []
